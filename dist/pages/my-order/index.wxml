<block wx:if="{{$taroCompReady}}">
    <view class="page order__root bg-gray">
        <at-tabs compid="{{$compid__279}}"></at-tabs>
        <block>
            <block wx:if="{{listData.length > 0}}">
                <view class="order__list">
                    <view class="order__list-item" wx:key="orderId" wx:for="{{loopArray38}}" wx:for-item="order" wx:for-index="_anonIdx2">
                        <order-card compid="{{order.$compid__278}}"></order-card>
                    </view>
                </view>
            </block>
            <block wx:else>
                <empty-data compid="{{$compid__280}}">您还没有{{anonymousState__temp}}我相信就在今天</empty-data>
            </block>
        </block>
        <tab-bar compid="{{$compid__281}}"></tab-bar>
    </view>
</block>
<block wx:if="{{$taroCompReady}}">
    <view class="network-error__root">
        <view class="img">
            <image src="{{imgUrl}}" mode="widthFix" style="width:200px"></image>
        </view>
        <view class="network-error__content text-center">{{content}}</view>
        <view class="text-center">
            <at-button compid="{{$compid__456}}">刷新</at-button>
        </view>
    </view>
</block>
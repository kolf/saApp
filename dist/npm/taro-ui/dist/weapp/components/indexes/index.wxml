<block wx:if="{{$taroCompReady}}">
    <view class="{{rootCls}}" style="{{anonymousState__temp}}">
        <at-toast compid="{{$compid__469}}"></at-toast>
        <view class="at-indexes__menu" catchtouchmove="handleTouchMove" bindtouchend="handleTouchEnd">
            <view class="at-indexes__menu-item" bindtap="jumpTarget" data-e-tap-so="this" data-e-tap-a-a="at-indexes__top" data-e-tap-a-b="{{0}}">{{topKey}}</view>
            <view class="at-indexes__menu-item" wx:key="key" bindtap="jumpTarget" wx:for="{{loopArray68}}" wx:for-item="dataList" wx:for-index="i" data-e-tap-so="this" data-e-tap-a-a="{{dataList.targetView}}" data-e-tap-a-b="{{i + 1}}">{{dataList.key}}</view>
        </view>
        <scroll-view class="at-indexes__body" id="{{listId}}" scroll-y="{{true}}" scroll-with-animation="{{animation}}" scroll-top="{{isWEB ? _scrollTop : undefined}}" scroll-into-view="{{!isWEB ? _scrollIntoView : ''}}" bindscroll="handleScroll" data-e-scroll-so="this">
            <view class="at-indexes__content" id="at-indexes__top">
                <slot></slot>
            </view>
            <view id="{{'at-indexes__list-' + dataList.$original.key}}" class="at-indexes__list" wx:key="key" wx:for="{{loopArray69}}" wx:for-item="dataList" wx:for-index="_anonIdx4">
                <view class="at-indexes__list-title">{{dataList.$original.title}}</view>
                <at-list>
                    <block wx:if="{{dataList.$original.items}}">
                        <at-list-item wx:key="name" wx:for="{{dataList.$anonymousCallee__28}}" wx:for-item="item" wx:for-index="_anonIdx2" compid="{{item.$compid__468}}"></at-list-item>
                    </block>
                </at-list>
            </view>
        </scroll-view>
    </view>
</block>